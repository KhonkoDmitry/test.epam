<?php

$route = isset($_GET['route']) ? $_GET['route'] : 'home';
$id_worker = isset($_GET['id_worker']) ? $_GET['id_worker'] : '';
$order = isset($_GET['order']) ? $_GET['order'] : '';
$sort = isset($_GET['sort']) ? $_GET['sort'] : '';
$salary = isset($_GET['salary']) ? $_GET['salary'] : '';